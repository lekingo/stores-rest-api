meta {
  name: /item Get items
  type: http
  seq: 6
}

get {
  url: {{storeURL}}/item
  body: none
  auth: bearer
}

auth:bearer {
  token: {{refresh_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
